---
page_title: "unifi_setting_usg Resource - unifi"
subcategory: ""
description: |-
  Manages UniFi site USG/gateway settings.
---

# unifi_setting_usg (Resource)

Manages UniFi site USG/gateway settings (UPnP, mDNS, NAT modules, offloading, etc.). This is a singleton resource — one per site. Destroying resets to defaults.

## Example Usage

```terraform
# Configure site USG/gateway settings
resource "unifi_setting_usg" "example" {
  broadcast_ping = false
  mdns_enabled   = true
  upnp_enabled   = true
  upnp_secure_mode = true
  lldp_enable_all = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `broadcast_ping` (Boolean)
- `dhcpd_use_dnsmasq` (Boolean)
- `ftp_module` (Boolean)
- `gre_module` (Boolean)
- `h323_module` (Boolean)
- `lldp_enable_all` (Boolean)
- `mdns_enabled` (Boolean)
- `mss_clamp` (String) MSS clamping mode: 'auto' or 'custom'.
- `mss_clamp_mss` (Number) MSS clamp value (when mss_clamp is 'custom').
- `offload_accounting` (Boolean)
- `offload_l2_blocking` (Boolean)
- `offload_sch` (Boolean)
- `pptp_module` (Boolean)
- `receive_redirects` (Boolean)
- `send_redirects` (Boolean)
- `sip_module` (Boolean)
- `syn_cookies` (Boolean)
- `tftp_module` (Boolean)
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `upnp_enabled` (Boolean)
- `upnp_nat_pmp_enabled` (Boolean)
- `upnp_secure_mode` (Boolean)

### Read-Only

- `id` (String) The ID of this resource.
- `site_id` (String)

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

USG settings can be imported using the setting ID:

```shell
terraform import unifi_setting_usg.example 60a1b2c3d4e5f67890123456
```
