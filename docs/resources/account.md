---
page_title: "unifi_account Resource - unifi"
subcategory: ""
description: |-
  Manages a UniFi RADIUS account.
---

# unifi_account (Resource)

Manages a UniFi RADIUS account for 802.1X authentication, VPN access, or custom tunnel configurations.

~> **Note:** The `x_password` attribute is write-only. After import, you must re-apply to set the password.

## Example Usage

```terraform
# Create a RADIUS account for 802.1X authentication
resource "unifi_account" "wireless_user" {
  name       = "jsmith"
  x_password = "secure-password-123"
}

# Create a RADIUS account with VLAN assignment
resource "unifi_account" "vlan_user" {
  name       = "guest-user"
  x_password = "guest-pass-456"
  vlan       = 100
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The username for the RADIUS account.
- `x_password` (String, Sensitive) The password for the RADIUS account (write-only).

### Optional

- `network_id` (String) The network configuration ID.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `tunnel_config_type` (String) Tunnel configuration type. Valid values: '802.1x', 'vpn', 'custom'.
- `tunnel_medium_type` (Number) Tunnel medium type (1-15).
- `tunnel_type` (Number) Tunnel type (1-13).
- `vlan` (Number) VLAN ID (2-4009).

### Read-Only

- `id` (String) The unique identifier of the RADIUS account.
- `site_id` (String) The site ID.

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Accounts can be imported using their ID:

```shell
terraform import unifi_account.example 60a1b2c3d4e5f67890123456
```
