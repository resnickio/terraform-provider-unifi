---
page_title: "unifi_setting_mgmt Resource - unifi"
subcategory: ""
description: |-
  Manages UniFi site management settings.
---

# unifi_setting_mgmt (Resource)

Manages UniFi site management settings (auto-upgrade, LED, SSH, alerts). This is a singleton resource — one per site. Destroying resets to defaults.

~> **Note:** The `x_ssh_password` attribute is write-only. After import, you must re-apply to set the password.

## Example Usage

```terraform
# Configure site management settings
resource "unifi_setting_mgmt" "example" {
  auto_upgrade = true
  led_enabled  = true
  alert_enabled = true
  x_ssh_enabled = true
  x_ssh_auth_password_enabled = true
  x_ssh_username = "admin"
  x_ssh_password = "secure-ssh-pass"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `alert_enabled` (Boolean) Enable alerts.
- `auto_upgrade` (Boolean) Enable automatic device firmware upgrades.
- `auto_upgrade_hour` (Number) Hour of day for auto-upgrades (0-23).
- `led_enabled` (Boolean) Enable device LEDs.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `x_ssh_auth_password_enabled` (Boolean) Enable SSH password authentication.
- `x_ssh_enabled` (Boolean) Enable SSH access to devices.
- `x_ssh_password` (String, Sensitive) SSH password (write-only).
- `x_ssh_username` (String) SSH username.

### Read-Only

- `id` (String) The ID of this resource.
- `site_id` (String)

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Management settings can be imported using the setting ID:

```shell
terraform import unifi_setting_mgmt.example 60a1b2c3d4e5f67890123456
```
