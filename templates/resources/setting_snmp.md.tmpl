---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
  Manages UniFi SNMP settings.
---

# {{.Name}} ({{.Type}})

Manages UniFi SNMP settings. This is a singleton resource — one per site. Destroying resets to defaults.

~> **Note:** The `x_password` attribute is write-only. After import, you must re-apply to set the password.

## Example Usage

{{tffile "examples/resources/unifi_setting_snmp/resource.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

SNMP settings can be imported using the setting ID:

```shell
terraform import unifi_setting_snmp.example 60a1b2c3d4e5f67890123456
```
