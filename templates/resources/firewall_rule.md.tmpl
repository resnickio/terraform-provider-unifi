---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
  Manages a UniFi legacy firewall rule.
---

# {{.Name}} ({{.Type}})

Manages a UniFi legacy firewall rule.

~> **Note:** This resource is for legacy firewall rules. On UDM devices with Network 10.x+, use `unifi_firewall_policy` instead for zone-based firewall rules.

## Example Usage

{{tffile "examples/resources/unifi_firewall_rule/resource.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Firewall rules can be imported using their ID:

```shell
terraform import unifi_firewall_rule.example 60a1b2c3d4e5f67890123456
```
